local Players=game:GetService("Players")
local TweenService=game:GetService("TweenService")
local UserInputService=game:GetService("UserInputService")
local RunService=game:GetService("RunService")
local player=Players.LocalPlayer

local desyncOn=false
local autoOn=false
local speed=10
local autoConn=nil

local AC=Color3.fromRGB(120,80,255)
local BG=Color3.fromRGB(10,10,18)
local BM=Color3.fromRGB(18,18,30)
local WH=Color3.fromRGB(255,255,255)
local GR=Color3.fromRGB(160,160,170)
local RD=Color3.fromRGB(220,60,60)
local GN=Color3.fromRGB(80,220,80)

local function mkCorner(p,r) local c=Instance.new("UICorner",p) c.CornerRadius=UDim.new(0,r or 12) end
local function mkStroke(p,col,th) local s=Instance.new("UIStroke",p) s.Color=col or AC s.Thickness=th or 2 return s end

local function makeDraggable(frame,handle)
    local drag,ds,dp=false,nil,nil
    handle.InputBegan:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then
            drag=true ds=i.Position dp=frame.Position
            i.Changed:Connect(function()
                if i.UserInputState==Enum.UserInputState.End then drag=false end
            end)
        end
    end)
    UserInputService.InputChanged:Connect(function(i)
        if drag and(i.UserInputType==Enum.UserInputType.MouseMovement or i.UserInputType==Enum.UserInputType.Touch) then
            local d=i.Position-ds
            frame.Position=UDim2.new(dp.X.Scale,dp.X.Offset+d.X,dp.Y.Scale,dp.Y.Offset+d.Y)
        end
    end)
end

-- ========================
-- DESYNC LOGIC
-- ========================
local function activateDesync()
    if desyncOn then return end
    desyncOn=true
    task.spawn(function()
        local char=player.Character if not char then return end
        local hum=char:FindFirstChildOfClass("Humanoid") if not hum then return end
        local cloner=player.Backpack:FindFirstChild("Quantum Cloner") or char:FindFirstChild("Quantum Cloner")
        if cloner then
            pcall(function() hum:EquipTool(cloner) end)
            task.wait(0.1)
            pcall(function() cloner:Activate() end)
            task.wait(0.1)
        end
        local cloneName=tostring(player.UserId).."_Clone"
        for _=1,30 do
            if workspace:FindFirstChild(cloneName) then break end
            task.wait(0.1)
        end
        local clone=workspace:FindFirstChild(cloneName)
        if clone then
            local cloneHRP=clone:FindFirstChild("HumanoidRootPart")
            if cloneHRP then
                local hrp=char:FindFirstChild("HumanoidRootPart")
                if hrp then pcall(function() hrp.CFrame=cloneHRP.CFrame end) end
            end
            task.wait(0.3)
        end
        pcall(function() raknet.desync(true) end)
        task.wait(0.2)
        pcall(function() hum:UnequipTools() end)
    end)
end

local function deactivateDesync()
    if not desyncOn then return end
    desyncOn=false
    pcall(function() raknet.desync(false) end)
end

-- ========================
-- AUTO DETECT CLONER
-- ========================
local function startAuto()
    if autoConn then autoConn:Disconnect() autoConn=nil end
    autoConn=RunService.Heartbeat:Connect(function()
        if not autoOn then return end
        local char=player.Character if not char then return end
        -- Check si le cloner est équipé dans les mains
        local cloner=char:FindFirstChild("Quantum Cloner")
        if cloner and not desyncOn then
            activateDesync()
        elseif not cloner and desyncOn then
            deactivateDesync()
        end
    end)
end

-- ========================
-- GUI
-- ========================
local sg=Instance.new("ScreenGui",player.PlayerGui)
sg.Name="UchiwaDesync" sg.ResetOnSpawn=false

local main=Instance.new("Frame",sg)
main.Size=UDim2.new(0,260,0,280)
main.Position=UDim2.new(0.5,-130,0.5,-140)
main.BackgroundColor3=BG main.BackgroundTransparency=0.08 main.BorderSizePixel=0
mkCorner(main,16)
local ms=mkStroke(main,AC,2.5)

-- TOP HANDLE (draggable)
local topHandle=Instance.new("Frame",main)
topHandle.Size=UDim2.new(1,0,0,50) topHandle.BackgroundTransparency=1 topHandle.BorderSizePixel=0
makeDraggable(main,topHandle)

-- TITLE
local titleLbl=Instance.new("TextLabel",main)
titleLbl.Size=UDim2.new(1,-20,0,50) titleLbl.Position=UDim2.new(0,10,0,0)
titleLbl.BackgroundTransparency=1 titleLbl.Text="Uchiwa Desync"
titleLbl.TextColor3=WH titleLbl.Font=Enum.Font.GothamBold titleLbl.TextSize=22
titleLbl.TextXAlignment=Enum.TextXAlignment.Center

-- Dots décoratifs
local function makeDot(x,y)
    local d=Instance.new("Frame",main)
    d.Size=UDim2.new(0,8,0,8) d.Position=UDim2.new(0,x,0,y)
    d.BackgroundColor3=WH d.BackgroundTransparency=0.6 d.BorderSizePixel=0 mkCorner(d,10)
end
makeDot(14,14) makeDot(230,14) makeDot(14,240) makeDot(230,240)

-- ON/OFF BUTTON
local onBtn=Instance.new("TextButton",main)
onBtn.Size=UDim2.new(1,-30,0,62) onBtn.Position=UDim2.new(0,15,0,58)
onBtn.BackgroundColor3=RD onBtn.BackgroundTransparency=0.1
onBtn.Text="OFF" onBtn.TextColor3=WH
onBtn.Font=Enum.Font.GothamBlack onBtn.TextSize=28
onBtn.BorderSizePixel=0 onBtn.AutoButtonColor=false
mkCorner(onBtn,14)

-- AUTO BUTTON
local autoBtn=Instance.new("TextButton",main)
autoBtn.Size=UDim2.new(1,-30,0,56) autoBtn.Position=UDim2.new(0,15,0,130)
autoBtn.BackgroundColor3=AC autoBtn.BackgroundTransparency=0.1
autoBtn.Text="AUTO: OFF" autoBtn.TextColor3=WH
autoBtn.Font=Enum.Font.GothamBlack autoBtn.TextSize=22
autoBtn.BorderSizePixel=0 autoBtn.AutoButtonColor=false
mkCorner(autoBtn,14)

-- SPEED BOX
local speedFrame=Instance.new("Frame",main)
speedFrame.Size=UDim2.new(1,-30,0,56) speedFrame.Position=UDim2.new(0,15,0,196)
speedFrame.BackgroundColor3=Color3.fromRGB(8,8,15) speedFrame.BackgroundTransparency=0.1
speedFrame.BorderSizePixel=0 mkCorner(speedFrame,14)

local speedBox=Instance.new("TextBox",speedFrame)
speedBox.Size=UDim2.new(1,0,1,0) speedBox.BackgroundTransparency=1
speedBox.Text="10" speedBox.TextColor3=GN
speedBox.Font=Enum.Font.GothamBlack speedBox.TextSize=26
speedBox.ClearTextOnFocus=false speedBox.BorderSizePixel=0
speedBox.TextXAlignment=Enum.TextXAlignment.Center
speedBox.FocusLost:Connect(function()
    local n=tonumber(speedBox.Text)
    if n and n>0 then speed=n else speedBox.Text=tostring(speed) end
end)

-- ========================
-- BOUTON LOGIQUE
-- ========================
local function updateOnBtn()
    if desyncOn then
        onBtn.Text="ON"
        TweenService:Create(onBtn,TweenInfo.new(0.2),{BackgroundColor3=GN}):Play()
        TweenService:Create(ms,TweenInfo.new(0.2),{Color=GN}):Play()
        titleLbl.Text="Uchiwa Desync [ON]"
    else
        onBtn.Text="OFF"
        TweenService:Create(onBtn,TweenInfo.new(0.2),{BackgroundColor3=RD}):Play()
        TweenService:Create(ms,TweenInfo.new(0.2),{Color=AC}):Play()
        titleLbl.Text="Uchiwa Desync"
    end
end

onBtn.MouseButton1Click:Connect(function()
    if desyncOn then
        deactivateDesync()
    else
        activateDesync()
    end
    updateOnBtn()
end)

autoBtn.MouseButton1Click:Connect(function()
    autoOn=not autoOn
    if autoOn then
        autoBtn.Text="AUTO: ON"
        TweenService:Create(autoBtn,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(100,50,200)}):Play()
        startAuto()
    else
        autoBtn.Text="AUTO: OFF"
        TweenService:Create(autoBtn,TweenInfo.new(0.2),{BackgroundColor3=AC}):Play()
        if autoConn then autoConn:Disconnect() autoConn=nil end
    end
end)

-- Update visuel auto quand la desync change via auto
task.spawn(function()
    local lastState=desyncOn
    while main.Parent do
        if desyncOn~=lastState then
            lastState=desyncOn
            updateOnBtn()
        end
        task.wait(0.1)
    end
end)

-- Animation entrée
main.Position=UDim2.new(1.5,0,0.5,-140)
TweenService:Create(main,TweenInfo.new(0.4,Enum.EasingStyle.Back,Enum.EasingDirection.Out),{
    Position=UDim2.new(0.5,-130,0.5,-140)
}):Play()
