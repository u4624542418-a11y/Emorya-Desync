local Players=game:GetService("Players")
local TweenService=game:GetService("TweenService")
local UserInputService=game:GetService("UserInputService")
local RunService=game:GetService("RunService")
local player=Players.LocalPlayer

local AC=Color3.fromRGB(120,80,255)
local GN=Color3.fromRGB(80,220,80)

local function makeDraggable(frame)
    local drag,ds,dp=false,nil,nil
    frame.InputBegan:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then
            drag=true ds=i.Position dp=frame.Position
            i.Changed:Connect(function()
                if i.UserInputState==Enum.UserInputState.End then drag=false end
            end)
        end
    end)
    UserInputService.InputChanged:Connect(function(i)
        if drag and(i.UserInputType==Enum.UserInputType.MouseMovement or i.UserInputType==Enum.UserInputType.Touch) then
            local d=i.Position-ds
            frame.Position=UDim2.new(dp.X.Scale,dp.X.Offset+d.X,dp.Y.Scale,dp.Y.Offset+d.Y)
        end
    end)
end

-- GUI
local sg=Instance.new("ScreenGui",player.PlayerGui)
sg.Name="UchiwaDesync" sg.ResetOnSpawn=false

local main=Instance.new("Frame",sg)
main.Size=UDim2.new(0,70,0,25)
main.Position=UDim2.new(0.5,-35,0.5,-12)
main.BackgroundColor3=Color3.fromRGB(10,10,18)
main.BackgroundTransparency=0.1 main.BorderSizePixel=0
local co=Instance.new("UICorner",main) co.CornerRadius=UDim.new(0,8)
local ms=Instance.new("UIStroke",main) ms.Color=AC ms.Thickness=1.5
makeDraggable(main)

local lbl=Instance.new("TextLabel",main)
lbl.Size=UDim2.new(1,0,1,0) lbl.BackgroundTransparency=1
lbl.Text="DESYNC" lbl.TextColor3=Color3.fromRGB(160,160,170)
lbl.Font=Enum.Font.GothamBlack lbl.TextSize=11 lbl.BorderSizePixel=0

-- DESYNC SANS DELAY
task.spawn(function()
    local char=player.Character or player.CharacterAdded:Wait()
    local hum=char:FindFirstChildOfClass("Humanoid") or char:WaitForChild("Humanoid")

    -- Equipe le cloner
    local cloner=player.Backpack:FindFirstChild("Quantum Cloner") or char:FindFirstChild("Quantum Cloner")
    if cloner then
        pcall(function() hum:EquipTool(cloner) end)
        pcall(function() cloner:Activate() end)
    end

    -- Attend le bouton "Change With Clone" via Heartbeat — 0 delay fixe
    local conn
    conn=RunService.Heartbeat:Connect(function()
        -- Cherche dans PlayerGui
        for _,v in pairs(player.PlayerGui:GetDescendants()) do
            if (v:IsA("TextButton") or v:IsA("ImageButton")) then
                local txt=v.Text or ""
                local lbl2=v:FindFirstChildWhichIsA("TextLabel",true)
                local txt2=lbl2 and lbl2.Text or ""
                if txt:lower():find("change") or txt2:lower():find("change") then
                    conn:Disconnect()
                    pcall(function() firesignal(v.MouseButton1Click) end)
                    pcall(function() firesignal(v.Activated) end)
                    -- Desync immédiat
                    pcall(function() raknet.desync(true) end)
                    pcall(function() hum:UnequipTools() end)
                    -- Vert
                    lbl.TextColor3=GN
                    TweenService:Create(ms,TweenInfo.new(0.2),{Color=GN}):Play()
                    TweenService:Create(main,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(10,30,10)}):Play()
                    return
                end
            end
        end
        -- Cherche aussi ProximityPrompt
        for _,v in pairs(workspace:GetDescendants()) do
            if v:IsA("ProximityPrompt") and v.ActionText:lower():find("change") then
                conn:Disconnect()
                pcall(function() fireproximityprompt(v) end)
                pcall(function() raknet.desync(true) end)
                pcall(function() hum:UnequipTools() end)
                lbl.TextColor3=GN
                TweenService:Create(ms,TweenInfo.new(0.2),{Color=GN}):Play()
                TweenService:Create(main,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(10,30,10)}):Play()
                return
            end
        end
    end)
end)
